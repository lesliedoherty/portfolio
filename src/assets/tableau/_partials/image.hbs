<figure class="image {{ modifier_class }}">
  <img src="{{ src }}" alt="{{ alt }}" />
  {{#with caption}}
    <figcaption class="caption caption--image {{ class }}">{{{ text }}}</figcaption>
  {{/with}}
</figure>
